package pkg

type ScriptGenerator struct {
}

func (*ScriptGenerator) GenerateGet(modelName string) error {
	var err error

	rootPath := Find
	return err
}

const (
	entitiyScript = `package entities
	type User struct {
		ID       int64
	}`
)
